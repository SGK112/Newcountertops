<!-- Breadcrumb -->
<div class="breadcrumb-section">
    <div class="container">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/">Home</a></li>
                <li class="breadcrumb-item active" aria-current="page">Countertops</li>
            </ol>
        </nav>
    </div>
</div>

<!-- Hero Section -->
<section class="page-hero">
    <div class="container">
        <div class="hero-content">
            <div class="hero-main">
                <h1 class="hero-title">Discover Premium Countertops</h1>
                <p class="hero-subtitle">Browse our curated collection of professional-grade countertops and connect with certified fabricators.</p>
                
                <div class="hero-features">
                    <div class="feature-item">
                        <span class="feature-icon">✓</span>
                        <span>Premium quality materials</span>
                    </div>
                    <div class="feature-item">
                        <span class="feature-icon">✓</span>
                        <span>Certified fabricators</span>
                    </div>
                    <div class="feature-item">
                        <span class="feature-icon">✓</span>
                        <span>Professional installation</span>
                    </div>
                </div>
                
                <div class="hero-cta">
                    <a href="/get-quote" class="btn btn-primary btn-xl">Get Professional Quote</a>
                    <span class="cta-subtitle">Free consultation included</span>
                </div>
            </div>
            
            <div class="hero-image">
                <img src="/images/hero-countertops.svg" alt="Premium countertop installation" 
                     onerror="this.style.display='none'">
            </div>
        </div>
        
        <!-- Trust bar -->
        <div class="trust-bar">
            <div class="trust-item">
                <span class="trust-number">500K+</span>
                <span class="trust-label">Projects completed</span>
            </div>
            <div class="trust-item">
                <span class="trust-number">25K+</span>
                <span class="trust-label">Certified professionals</span>
            </div>
            <div class="trust-item">
                <span class="trust-number">4.9★</span>
                <span class="trust-label">Customer rating</span>
            </div>
            <div class="trust-item">
                <span class="trust-number">15+</span>
                <span class="trust-label">Years experience</span>
            </div>
        </div>
    </div>
</section>

<!-- Filter Section -->
<section class="filter-section">
    <div class="container">
        <div class="filter-bar">
            <div class="filter-group">
                <label>Material</label>
                <select id="materialFilter" class="form-select">
                    <option value="">All Materials</option>
                    <option value="granite">Granite</option>
                    <option value="quartz">Quartz</option>
                    <option value="marble">Marble</option>
                    <option value="quartzite">Quartzite</option>
                    <option value="concrete">Concrete</option>
                </select>
            </div>
            
            <div class="filter-group">
                <label>Price Range</label>
                <select id="priceFilter" class="form-select">
                    <option value="">Any Price</option>
                    <option value="0-50">$0 - $50/sq ft</option>
                    <option value="50-80">$50 - $80/sq ft</option>
                    <option value="80-120">$80 - $120/sq ft</option>
                    <option value="120+">$120+/sq ft</option>
                </select>
            </div>
            
            <div class="filter-group">
                <label>Color</label>
                <select id="colorFilter" class="form-select">
                    <option value="">Any Color</option>
                    <option value="white">White</option>
                    <option value="black">Black</option>
                    <option value="gray">Gray</option>
                    <option value="brown">Brown</option>
                    <option value="gold">Gold</option>
                </select>
            </div>
            
            <div class="filter-group">
                <label>Sort By</label>
                <select id="sortFilter" class="form-select">
                    <option value="featured">Featured</option>
                    <option value="price-low">Price: Low to High</option>
                    <option value="price-high">Price: High to Low</option>
                    <option value="popularity">Most Popular</option>
                    <option value="newest">Newest</option>
                </select>
            </div>
            
            <button class="btn btn-outline filter-reset" onclick="resetFilters()">Clear All</button>
        </div>
    </div>
</section>

<!-- Popular Countertops Section -->
<section class="popular-section">
    <div class="container">
        <h2 class="section-title">Popular countertop services</h2>
        <div class="popular-grid">
            <a href="/get-quote?material=granite" class="popular-card">
                <div class="popular-image">
                    <img src="/images/granite-sample.svg" alt="Granite countertops" onerror="this.style.display='none'">
                </div>
                <div class="popular-content">
                    <h3>Granite Installation</h3>
                    <div class="rating">4.7 ★ (125k+)</div>
                    <div class="price">from $65/sq ft</div>
                </div>
            </a>
            <a href="/get-quote?material=quartz" class="popular-card">
                <div class="popular-image">
                    <img src="/images/quartz-sample.svg" alt="Quartz countertops" onerror="this.style.display='none'">
                </div>
                <div class="popular-content">
                    <h3>Quartz Installation</h3>
                    <div class="rating">4.8 ★ (98k+)</div>
                    <div class="price">from $75/sq ft</div>
                </div>
            </a>
            <a href="/get-quote?material=marble" class="popular-card">
                <div class="popular-image">
                    <img src="/images/marble-sample.svg" alt="Marble countertops" onerror="this.style.display='none'">
                </div>
                <div class="popular-content">
                    <h3>Marble Installation</h3>
                    <div class="rating">4.6 ★ (67k+)</div>
                    <div class="price">from $85/sq ft</div>
                </div>
            </a>
            <a href="/get-quote?material=quartzite" class="popular-card">
                <div class="popular-image">
                    <img src="/images/quartzite-sample.svg" alt="Quartzite countertops" onerror="this.style.display='none'">
                </div>
                <div class="popular-content">
                    <h3>Quartzite Installation</h3>
                    <div class="rating">4.7 ★ (43k+)</div>
                    <div class="price">from $90/sq ft</div>
                </div>
            </a>
        </div>
    </div>
</section>

<!-- Browse All Section -->
<section class="browse-section">
    <div class="container">
        <div class="section-header">
            <h2>Browse all countertops</h2>
            <p>Explore thousands of options from verified fabricators</p>
        </div>
        <div class="results-header">
            <div class="results-count">
                <span id="resultsCount">Loading...</span> countertops found
            </div>
            <div class="view-toggle">
                <button class="view-btn active" data-view="grid">
                    Grid View
                </button>
                <button class="view-btn" data-view="list">
                    List View
                </button>
            </div>
        </div>
        
        <!-- Loading State -->
        <div class="loading-state" id="loadingState">
            <div class="skeleton-cards">
                <div class="skeleton-card"></div>
                <div class="skeleton-card"></div>
                <div class="skeleton-card"></div>
                <div class="skeleton-card"></div>
                <div class="skeleton-card"></div>
                <div class="skeleton-card"></div>
            </div>
        </div>
        
        <!-- Countertops Grid -->
        <div class="countertops-grid" id="countertopsGrid">
            <!-- Countertops will be loaded here via JavaScript -->
        </div>
        
        <!-- Empty State -->
        <div class="empty-state d-none" id="emptyState">
            <div class="empty-icon">🔍</div>
            <h3>No countertops found</h3>
            <p>Try adjusting your filters or <a href="#" onclick="resetFilters()">clear all filters</a> to see more results.</p>
        </div>
        
        <!-- Load More -->
        <div class="load-more-section" id="loadMoreSection">
            <button class="btn btn-outline btn-lg" id="loadMoreBtn">Load More Countertops</button>
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="cta-section">
    <div class="container">
        <div class="cta-content">
            <h2>Ready to start your countertop project?</h2>
            <p>Get matched with top-rated fabricators in your area. Free quotes, no obligations.</p>
            <a href="/get-quote" class="btn btn-white btn-lg">Get Free Quotes Now</a>
            <div class="cta-features">
                <span>✓ Compare multiple quotes</span>
                <span>✓ Licensed & insured pros</span>
                <span>✓ 100% satisfaction guarantee</span>
            </div>
        </div>
    </div>
</section>

<!-- How It Works Section -->
<section class="how-it-works-section">
    <div class="container">
        <h2 class="section-title">How NewCountertops works</h2>
        <div class="steps-grid">
            <div class="step-item">
                <div class="step-number">1</div>
                <h3>Tell us about your project</h3>
                <p>Answer a few quick questions about your countertop needs and preferences.</p>
            </div>
            <div class="step-item">
                <div class="step-number">2</div>
                <h3>Get matched with pros</h3>
                <p>We'll connect you with up to 3 verified fabricators in your area.</p>
            </div>
            <div class="step-item">
                <div class="step-number">3</div>
                <h3>Compare and hire</h3>
                <p>Review quotes, ratings, and portfolios to choose the best pro for your project.</p>
            </div>
        </div>
    </div>
</section>

<!-- Testimonials Section -->
<section class="testimonials-section">
    <div class="container">
        <h2 class="section-title">What our customers are saying</h2>
        <div class="testimonials-grid">
            <div class="testimonial-card">
                <div class="testimonial-rating">★★★★★</div>
                <p>"Amazing experience! Got 3 quotes within 24 hours and found the perfect fabricator for our granite countertops."</p>
                <div class="testimonial-author">
                    <strong>Sarah M.</strong>
                    <span>Austin, TX</span>
                </div>
            </div>
            <div class="testimonial-card">
                <div class="testimonial-rating">★★★★★</div>
                <p>"The quality of pros on NewCountertops is excellent. Our quartz installation was flawless and on budget."</p>
                <div class="testimonial-author">
                    <strong>Mike D.</strong>
                    <span>Denver, CO</span>
                </div>
            </div>
            <div class="testimonial-card">
                <div class="testimonial-rating">★★★★★</div>
                <p>"Saved us thousands by comparing quotes. The fabricator we chose did an incredible job on our marble island."</p>
                <div class="testimonial-author">
                    <strong>Jennifer L.</strong>
                    <span>Seattle, WA</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
/* Professional Blue & Yellow theme for countertops page */
.page-hero {
    background: linear-gradient(135deg, #1e40af 0%, #3b82f6 100%);
    color: white;
    padding: 3rem 0 2rem 0;
}

.hero-content {
    display: flex;
    align-items: center;
    gap: 3rem;
    margin-bottom: 2rem;
}

.hero-main {
    flex: 1;
}

.hero-title {
    font-size: 2.5rem;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 1rem;
}

.hero-subtitle {
    font-size: 1.25rem;
    opacity: 0.9;
    margin-bottom: 2rem;
}

.hero-features {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-bottom: 2rem;
}

.feature-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 1rem;
}

.feature-icon {
    background: rgba(245, 158, 11, 0.9);
    border-radius: 50%;
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    font-weight: bold;
    color: white;
}

.hero-cta {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.5rem;
}

.btn-xl {
    padding: 1rem 2rem;
    font-size: 1.1rem;
    font-weight: 600;
    border-radius: 8px;
}

.cta-subtitle {
    font-size: 0.9rem;
    opacity: 0.8;
}

.hero-image {
    flex: 0 0 300px;
    height: 200px;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 8px 32px rgba(0,0,0,0.2);
}

.hero-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.trust-bar {
    display: flex;
    justify-content: space-around;
    padding: 1.5rem 0;
    border-top: 1px solid rgba(255, 255, 255, 0.2);
}

.trust-item {
    text-align: center;
}

.trust-number {
    display: block;
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 0.25rem;
    color: #fbbf24;
}

.trust-label {
    font-size: 0.9rem;
    opacity: 0.8;
}

.popular-section {
    padding: 4rem 0;
    background: #f8fafc;
}

.section-title {
    font-size: 2rem;
    font-weight: 700;
    text-align: center;
    margin-bottom: 3rem;
    color: #1e293b;
}

.popular-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin-bottom: 3rem;
}

.popular-card {
    background: white;
    border-radius: 12px;
    padding: 1.5rem;
    text-decoration: none;
    color: inherit;
    box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    transition: all 0.3s ease;
    border: 1px solid #e2e8f0;
}

.popular-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 25px rgba(30, 64, 175, 0.15);
    text-decoration: none;
    color: inherit;
}

.popular-image {
    width: 60px;
    height: 60px;
    border-radius: 8px;
    overflow: hidden;
    margin-bottom: 1rem;
    background: #f8fafc;
}

.popular-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.popular-content h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: #1e293b;
}

.rating {
    font-size: 0.9rem;
    color: #64748b;
    margin-bottom: 0.5rem;
}

.price {
    font-size: 1rem;
    font-weight: 600;
    color: #f59e0b;
}

.browse-section {
    padding: 3rem 0;
}

.section-header {
    text-align: center;
    margin-bottom: 3rem;
}

.section-header h2 {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    color: #1e293b;
}

.section-header p {
    font-size: 1.1rem;
    color: #64748b;
}

.filter-section {
    background: white;
    padding: 2rem 0;
    border-bottom: 1px solid #e2e8f0;
    border-top: 1px solid #e2e8f0;
}

.filter-bar {
    display: flex;
    gap: 1.5rem;
    align-items: end;
    flex-wrap: wrap;
    justify-content: center;
}

.filter-group {
    display: flex;
    flex-direction: column;
    min-width: 140px;
}

.filter-group label {
    font-weight: 500;
    margin-bottom: 0.5rem;
    color: #475569;
    font-size: 0.9rem;
}

.form-select {
    border: 2px solid #e2e8f0;
    border-radius: 8px;
    padding: 0.75rem;
    font-size: 0.95rem;
    transition: border-color 0.2s;
}

.form-select:focus {
    border-color: #1e40af;
    outline: none;
    box-shadow: 0 0 0 3px rgba(30, 64, 175, 0.1);
}

.results-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
    padding: 1rem 0;
}

.results-count {
    font-weight: 500;
    color: #475569;
    font-size: 1.1rem;
}

.countertops-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 1.5rem;
    margin-bottom: 3rem;
}

.countertop-card {
    background: white;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    transition: all 0.3s ease;
    cursor: pointer;
    border: 1px solid #e2e8f0;
}

.countertop-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 25px rgba(30, 64, 175, 0.15);
}

.countertop-image-container {
    position: relative;
    width: 100%;
    height: 220px;
    background: #f8fafc;
}

.countertop-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.countertop-image-placeholder {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    background: #f8fafc;
    color: #64748b;
}

.placeholder-icon {
    font-size: 2rem;
    margin-bottom: 0.5rem;
}

.placeholder-text {
    font-size: 0.9rem;
}

.countertop-content {
    padding: 1.5rem;
}

.countertop-title {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: #1e293b;
    line-height: 1.3;
}

.countertop-material {
    color: #64748b;
    font-size: 0.9rem;
    margin-bottom: 0.75rem;
    text-transform: capitalize;
}

.countertop-price {
    font-size: 1.2rem;
    font-weight: 700;
    color: #f59e0b;
    margin-bottom: 1rem;
}

.countertop-features {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 1rem;
}

.feature-tag {
    background: #f1f5f9;
    color: #475569;
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 500;
}

.countertop-actions {
    display: flex;
    gap: 0.75rem;
}

.btn-primary {
    background: #1e40af;
    border: 2px solid #1e40af;
    color: white;
    font-weight: 600;
}

.btn-primary:hover {
    background: #1e3a8a;
    border-color: #1e3a8a;
}

.btn-outline {
    background: transparent;
    border: 2px solid #e2e8f0;
    color: #475569;
    font-weight: 500;
}

.btn-outline:hover {
    background: #f8fafc;
    border-color: #cbd5e1;
}

.btn-sm {
    padding: 0.5rem 1rem;
    font-size: 0.875rem;
    border-radius: 6px;
}

.cta-section {
    background: linear-gradient(135deg, #1e40af 0%, #3b82f6 100%);
    color: white;
    padding: 4rem 0;
    text-align: center;
}

.cta-content {
    max-width: 600px;
    margin: 0 auto;
}

.cta-section h2 {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
}

.cta-section p {
    font-size: 1.2rem;
    margin-bottom: 2rem;
    opacity: 0.9;
}

.cta-features {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin-top: 2rem;
    flex-wrap: wrap;
}

.cta-features span {
    font-size: 0.95rem;
    opacity: 0.9;
}

.how-it-works-section {
    padding: 4rem 0;
    background: #f8fafc;
}

.steps-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
}

.step-item {
    text-align: center;
    padding: 2rem 1rem;
}

.step-number {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: #f59e0b;
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    font-weight: 700;
    margin: 0 auto 1.5rem auto;
}

.step-item h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: #1e293b;
}

.step-item p {
    color: #64748b;
    line-height: 1.6;
}

.testimonials-section {
    padding: 4rem 0;
    background: white;
}

.testimonials-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
}

.testimonial-card {
    background: #f8fafc;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.05);
}

.testimonial-rating {
    color: #f59e0b;
    font-size: 1.2rem;
    margin-bottom: 1rem;
}

.testimonial-card p {
    font-size: 1rem;
    line-height: 1.6;
    color: #475569;
    margin-bottom: 1.5rem;
    font-style: italic;
}

.testimonial-author {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
}

.testimonial-author strong {
    color: #1e293b;
    font-weight: 600;
}

.testimonial-author span {
    color: #64748b;
    font-size: 0.9rem;
}

.btn-white {
    background: white;
    color: #1e40af;
    border: 2px solid white;
    font-weight: 600;
}

.btn-white:hover {
    background: #f8fafc;
    color: #1e3a8a;
}

/* Responsive */
@media (max-width: 768px) {
    .hero-content {
        flex-direction: column;
        text-align: center;
        gap: 2rem;
    }
    
    .hero-title {
        font-size: 2rem;
    }
    
    .hero-image {
        flex: none;
        width: 100%;
        max-width: 300px;
    }
    
    .trust-bar {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 1rem;
    }
    
    .popular-grid {
        grid-template-columns: 1fr;
    }
    
    .filter-bar {
        flex-direction: column;
        align-items: stretch;
    }
    
    .filter-group {
        min-width: 100%;
    }
    
    .results-header {
        flex-direction: column;
        gap: 1rem;
        align-items: stretch;
    }
    
    .countertops-grid {
        grid-template-columns: 1fr;
    }
    
    .cta-section h2 {
        font-size: 2rem;
    }
    
    .cta-features {
        flex-direction: column;
        gap: 1rem;
    }
    
    .steps-grid {
        grid-template-columns: 1fr;
    }
    
    .testimonials-grid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 480px) {
    .hero-title {
        font-size: 1.75rem;
    }
    
    .hero-subtitle {
        font-size: 1.1rem;
    }
    
    .trust-bar {
        grid-template-columns: 1fr;
        gap: 1rem;
    }
    
    .section-title {
        font-size: 1.75rem;
    }
    
    .cta-section h2 {
        font-size: 1.75rem;
    }
}

/* Loading and empty states */
.skeleton-cards {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 1.5rem;
}

.skeleton-card {
    height: 400px;
    background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
    background-size: 200% 100%;
    animation: skeleton-loading 2s infinite;
    border-radius: 12px;
}

@keyframes skeleton-loading {
    0% { background-position: 200% 0; }
    100% { background-position: -200% 0; }
}

.empty-state {
    text-align: center;
    padding: 4rem 2rem;
    color: #64748b;
}

.empty-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
}

.empty-state h3 {
    color: #475569;
    margin-bottom: 1rem;
}

.load-more-section {
    text-align: center;
    padding: 2rem 0;
}

.d-none {
    display: none !important;
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Initialize countertops page
    const countertopsPage = new CountertopsPage();
    countertopsPage.init();
});

class CountertopsPage {
    constructor() {
        this.currentPage = 1;
        this.itemsPerPage = 12;
        this.filters = {};
        this.loading = false;
        this.hasMore = true;
    }
    
    init() {
        this.bindEvents();
        this.loadCountertops();
    }
    
    bindEvents() {
        // Filter change events
        document.getElementById('materialFilter').addEventListener('change', (e) => {
            this.filters.material = e.target.value;
            this.resetAndLoad();
        });
        
        document.getElementById('priceFilter').addEventListener('change', (e) => {
            this.filters.priceRange = e.target.value;
            this.resetAndLoad();
        });
        
        document.getElementById('colorFilter').addEventListener('change', (e) => {
            this.filters.color = e.target.value;
            this.resetAndLoad();
        });
        
        document.getElementById('sortFilter').addEventListener('change', (e) => {
            this.filters.sortBy = e.target.value;
            this.resetAndLoad();
        });
        
        // Load more button
        document.getElementById('loadMoreBtn').addEventListener('click', () => {
            this.loadCountertops(true);
        });
        
        // View toggle
        document.querySelectorAll('.view-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                document.querySelectorAll('.view-btn').forEach(b => b.classList.remove('active'));
                e.target.classList.add('active');
                this.toggleView(e.target.dataset.view);
            });
        });
    }
    
    resetAndLoad() {
        this.currentPage = 1;
        this.hasMore = true;
        document.getElementById('countertopsGrid').innerHTML = '';
        this.loadCountertops();
    }
    
    async loadCountertops(append = false) {
        if (this.loading || !this.hasMore) return;
        
        this.loading = true;
        this.showLoading(!append);
        
        try {
            const params = new URLSearchParams({
                page: this.currentPage,
                limit: this.itemsPerPage,
                ...this.filters
            });
            
            const response = await fetch(`/api/countertops?${params}`);
            const data = await response.json();
            
            if (data.countertops) {
                this.renderCountertops(data.countertops, append);
                this.updateResultsCount(data.pagination.totalItems);
                this.hasMore = data.pagination.hasNextPage;
                this.currentPage++;
                
                if (!this.hasMore) {
                    document.getElementById('loadMoreSection').style.display = 'none';
                }
            } else {
                this.showEmptyState();
            }
        } catch (error) {
            console.error('Error loading countertops:', error);
            this.showError();
        }
        
        this.loading = false;
        this.hideLoading();
    }
    
    renderCountertops(countertops, append = false) {
        const grid = document.getElementById('countertopsGrid');
        
        if (!append) {
            grid.innerHTML = '';
        }
        
        if (countertops.length === 0 && !append) {
            this.showEmptyState();
            return;
        }
        
        countertops.forEach(countertop => {
            const card = this.createCountertopCard(countertop);
            grid.appendChild(card);
        });
        
        document.getElementById('emptyState').classList.add('d-none');
    }
    
    createCountertopCard(countertop) {
        const card = document.createElement('div');
        card.className = 'countertop-card';
        card.onclick = () => window.location.href = `/countertops/${countertop._id}`;
        
        const primaryImage = countertop.images?.find(img => img.isPrimary) || countertop.images?.[0];
        const imageUrl = primaryImage?.url || '/images/placeholder-countertop.svg';
        
        card.innerHTML = `
            <div class="countertop-image-container">
                <img src="${imageUrl}" alt="${countertop.name}" class="countertop-image" 
                     onerror="this.style.display='none'; this.nextElementSibling.style.display='flex'">
                <div class="countertop-image-placeholder" style="display: none;">
                    <div class="placeholder-icon">🏠</div>
                    <div class="placeholder-text">Countertop Image</div>
                </div>
            </div>
            <div class="countertop-content">
                <h3 class="countertop-title">${countertop.name}</h3>
                <div class="countertop-material">${this.formatMaterial(countertop.material)}</div>
                <div class="countertop-price">${countertop.pricePerSqFt || this.formatPriceRange(countertop.pricing?.priceRange)}</div>
                <div class="countertop-features">
                    ${countertop.properties?.maintenanceLevel ? `<span class="feature-tag">${countertop.properties.maintenanceLevel} maintenance</span>` : ''}
                    ${countertop.properties?.heatResistance ? `<span class="feature-tag">${countertop.properties.heatResistance} heat resistance</span>` : ''}
                    ${countertop.properties?.stainResistance ? `<span class="feature-tag">${countertop.properties.stainResistance} stain resistance</span>` : ''}
                </div>
                <div class="countertop-actions">
                    <button class="btn btn-primary btn-sm" onclick="event.stopPropagation(); requestQuote('${countertop._id}')">
                        Get Quote
                    </button>
                    <button class="btn btn-outline btn-sm" onclick="event.stopPropagation(); viewDetails('${countertop._id}')">
                        View Details
                    </button>
                </div>
            </div>
        `;
        
        return card;
    }
    
    formatMaterial(material) {
        return material.charAt(0).toUpperCase() + material.slice(1);
    }
    
    formatPriceRange(priceRange) {
        if (!priceRange) return 'Contact for pricing';
        const { min, max, unit } = priceRange;
        if (min && max) {
            return `$${min} - $${max}/${unit || 'sq ft'}`;
        }
        return 'Contact for pricing';
    }
    
    showLoading(full = false) {
        if (full) {
            document.getElementById('loadingState').classList.remove('d-none');
            document.getElementById('countertopsGrid').classList.add('d-none');
        }
        document.getElementById('loadMoreBtn').textContent = 'Loading...';
        document.getElementById('loadMoreBtn').disabled = true;
    }
    
    hideLoading() {
        document.getElementById('loadingState').classList.add('d-none');
        document.getElementById('countertopsGrid').classList.remove('d-none');
        document.getElementById('loadMoreBtn').textContent = 'Load More Countertops';
        document.getElementById('loadMoreBtn').disabled = false;
    }
    
    showEmptyState() {
        document.getElementById('emptyState').classList.remove('d-none');
        document.getElementById('loadMoreSection').style.display = 'none';
    }
    
    showError() {
        // Could show a toast notification or error state
        console.error('Failed to load countertops');
    }
    
    updateResultsCount(total) {
        document.getElementById('resultsCount').textContent = total;
    }
    
    toggleView(view) {
        const grid = document.getElementById('countertopsGrid');
        if (view === 'list') {
            grid.classList.add('list-view');
        } else {
            grid.classList.remove('list-view');
        }
    }
}

// Global functions for card actions
function requestQuote(countertopId) {
    window.location.href = `/get-quote?countertop=${countertopId}`;
}

function viewDetails(countertopId) {
    window.location.href = `/countertops/${countertopId}`;
}

function resetFilters() {
    document.getElementById('materialFilter').value = '';
    document.getElementById('priceFilter').value = '';
    document.getElementById('colorFilter').value = '';
    document.getElementById('sortFilter').value = 'featured';
    
    const page = new CountertopsPage();
    page.filters = {};
    page.resetAndLoad();
}
</script>
